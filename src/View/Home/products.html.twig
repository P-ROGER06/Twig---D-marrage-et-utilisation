  {% extends 'layout.html.twig' %}

{% block content %}
    {% for product in products %}
        {{ product }}
        {% if loop.products == 3%}
        <br>
        {% endif %}
        {% endfor %}
        {{ dump(products) }}
{% endblock %}
